{
  "name": "ngrx-data",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "rollup --config ./rollup.config.js",
    "tsc": "tsc -p .",
    "build": "npm run tsc && npm run bundle",
    "deploy": "npm run build && cp -r index.html dist public && firebase deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gojs": "^2.0.12",
    "google-palette": "^1.1.0",
    "rollup-plugin-json": "^4.0.0"
  },
  "devDependencies": {
    "rollup": "^1.15.6",
    "rollup-plugin-commonjs": "^10.0.0",
    "rollup-plugin-node-resolve": "^5.0.3",
    "typescript": "^3.5.2"
  }
}
